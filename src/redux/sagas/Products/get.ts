import { call, put, takeLatest, select } from "redux-saga/effects";
import * as types from "redux/reducers/products/actionTypes";

function* products() {
  // yield takeLatest(types.GET_SCHEMA_GROUP, getShemaGroupSaga);
}

export default products;
